#ifndef MNKSOLVER_UTILS_H
#define MNKSOLVER_UTILS_H

int getMax(int first, int second) {
    return first >= second ? first : second;
}

int getMin(int first, int second) {
    return first <= second ? first : second;
}

#endif //MNKSOLVER_UTILS_H
